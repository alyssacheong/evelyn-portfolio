# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next/
out/
next-env.d.ts

# local env files (secrets — never commit)
.env
.env*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project asset folders
Among the Lotus and the Light/
Lolita-Tea-Party-Magazine/
Miniature Coral Garden Music Video/

# Raw artwork sources — kept out of git (backups live on Desktop/Downloads)
WhimsicalFairyPicnic3.png
mural installation photos/

# Working drop folder for raw source video/images (~1GB) — the processed,
# web-ready copies live in public/; never commit the originals
new/
new 2/
